import { Flex, Typography } from 'antd';

const { Title } = Typography;

// Placeholder — routes to /accounting/ledgers. Content to be built out.
const LedgersLanding = () => (
    <Flex vertical gap={8} className="px-2 py-4">
        <Title level={4}>Ledgers</Title>
    </Flex>
);

export default LedgersLanding;
